import { View, Text } from 'react-native';

export default function RoomsScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#000', justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ color: '#fff' }}>Rooms Screen</Text>
    </View>
  );
}
